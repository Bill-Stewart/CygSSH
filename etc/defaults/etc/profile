# NOTE: Do not edit this file using Windows Notepad!
# It requires linefeed line terminators.

if ! `/bin/tty -s`; then
  return
fi

# add -c to below command line (e.g., '/bin/posh -c') to run
# PowerShell Core instead of Windows PowerShell
/bin/posh
exit
